@rust-attr(derive(serde::Serialize, serde::Deserialize))
export struct Settings {
    first_run                 : bool,   // Default: True
    app_language              : string, // Default: ""
    restore_on_system_startup : bool,   // Default: False
    confirm_to_delete_pipe    : bool,   // Default: True
}